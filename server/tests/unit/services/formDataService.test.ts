import { FormDataService } from '../../../src/services/formDataService';

describe('FormDataService', () => {
  it('should create service instance', () => {
    const service = new FormDataService();
    expect(service).toBeDefined();
  });
});
